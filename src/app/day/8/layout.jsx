export const metadata = {
  title: 'Day 8 | eSports statistics',
  description: `Last 10 year historical statistiscs of eSports games worldwide `,
};

export default function PageLayout({ children }) {
  return children;
}
